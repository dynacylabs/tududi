# Tududi Environment Configuration

# Application Settings
NODE_ENV=development
HOST=0.0.0.0
PORT=3002
FRONTEND_URL=http://localhost:8080

# Database
DB_FILE=./backend/db/development.sqlite3

# Session
TUDUDI_SESSION_SECRET=your-secret-key-here-generate-with-crypto-randomBytes

# User Credentials (for initial setup)
TUDUDI_USER_EMAIL=admin@example.com
TUDUDI_USER_PASSWORD=your-password-here

# CORS
TUDUDI_ALLOWED_ORIGINS=http://localhost:8080,http://localhost:9292,http://127.0.0.1:8080,http://127.0.0.1:9292

# Optional Features
DISABLE_SCHEDULER=false
DISABLE_TELEGRAM=false

# Upload Path
TUDUDI_UPLOAD_PATH=./backend/uploads

# Google Calendar Integration (Optional)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3002/api/calendar/oauth/callback

# OIDC / SSO Configuration (for Authelia or other OIDC providers)
# Set to 'true' to enable OIDC authentication
OIDC_ENABLED=true

# OIDC Issuer URL (e.g., https://auth.example.com)
OIDC_ISSUER=https://auth.example.com

# OIDC Client ID (from your OIDC provider configuration)
OIDC_CLIENT_ID=tududi

# OIDC Client Secret (from your OIDC provider configuration)
OIDC_CLIENT_SECRET=your-client-secret-here

# OIDC Callback URL (where OIDC provider will redirect after authentication)
OIDC_CALLBACK_URL=http://localhost:3002/api/auth/oidc/callback

# Optional: Explicit OIDC endpoint URLs (for Authelia compatibility)
# If not provided, they will be discovered from the issuer
# OIDC_AUTHORIZATION_URL=https://auth.example.com/api/oidc/authorization
# OIDC_TOKEN_URL=https://auth.example.com/api/oidc/token
# OIDC_USER_INFO_URL=https://auth.example.com/api/oidc/userinfo
# OIDC_LOGOUT_URL=https://auth.example.com/logout

# Authelia Example Configuration:
# OIDC_ENABLED=true
# OIDC_ISSUER=https://auth.example.com
# OIDC_CLIENT_ID=tududi
# OIDC_CLIENT_SECRET=your-secret-from-authelia
# OIDC_CALLBACK_URL=https://tududi.example.com/api/auth/oidc/callback
# OIDC_AUTHORIZATION_URL=https://auth.example.com/api/oidc/authorization
# OIDC_TOKEN_URL=https://auth.example.com/api/oidc/token
# OIDC_USER_INFO_URL=https://auth.example.com/api/oidc/userinfo
# OIDC_LOGOUT_URL=https://auth.example.com/logout
